# libbox.aar

Положи сюда собранный `libbox.aar`:

```text
app/libs/libbox.aar
```

Gradle подключает AAR автоматически, только если файл существует.
Без этого файла приложение собирается, но speed-check через VLESS и VPN-запуск будут отключены.
